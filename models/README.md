# Models

Here the definitions for creating s3tables and postgres-tables are to be stored.

The model definitions are spread over subdirectories based on the dataset provider and the dataset, e.g. `ch.bfe/erneuerbarheizen/`.
