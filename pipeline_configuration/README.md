# Pipeline Configuration

Here we store the pipeline configurations for the "general" DAGs. These are yaml files containing the sequence of [tools](../toolset) that will be processed in a task of a DAG job.
